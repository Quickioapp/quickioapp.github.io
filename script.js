const search=document.getElementById('toolSearch');const cards=[...document.querySelectorAll('.tool-card')];const noResults=document.getElementById('noResults');const normalize=s=>(s||'').toLowerCase().normalize('NFD').replace(/[\u0300-\u036f]/g,'');if(search){const filter=()=>{const q=normalize(search.value.trim());let visible=[];cards.forEach(c=>{const hay=normalize((c.dataset.search||'')+' '+c.innerText);const show=!q||hay.includes(q);c.style.display=show?'flex':'none';if(show)visible.push(c)});if(noResults)noResults.hidden=visible.length>0||!q;return visible};search.addEventListener('input',filter);search.addEventListener('keydown',e=>{if(e.key==='Enter'){const visible=filter();if(visible[0])visible[0].click()}})}document.querySelectorAll('[data-category]').forEach(link=>{link.addEventListener('click',()=>{if(search){search.value=link.dataset.category;search.dispatchEvent(new Event('input'));}})});